{
    "version": "0.2.0",

    "configurations": [
        {
            "name": "Run Web App - Firefox",

            "type": "firefox",

            "request": "launch",

            "file": "${workspaceFolder}/index.html",

            "reAttach": true
        }
    ]
}